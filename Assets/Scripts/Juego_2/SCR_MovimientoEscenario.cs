using UnityEngine;

public class SCR_MovimientoEscenario : MonoBehaviour
{

    [Header("Referencias")]
    public SCR_RanaEquilibrio ranaEquilibrio;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
